{
  "description": "Stop guard for an in-flight subagent-driven-development run",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/sdd-idle-check.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
